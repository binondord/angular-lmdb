'use strict';

describe('myApp.movieList module', function() {

  beforeEach(module('myApp.movieList'));

  describe('movieList controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var movieListCtrl = $controller('MovieListCtrl');
      expect(movieListCtrl).toBeDefined();
    }));

  });
});
