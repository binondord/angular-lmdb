'use strict';

describe('myApp.movieAdd module', function() {

  beforeEach(module('myApp.movieAdd'));

  describe('movieAdd controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var movieAddCtrl = $controller('MovieAddCtrl');
      expect(movieAddCtrl).toBeDefined();
    }));

  });
});
